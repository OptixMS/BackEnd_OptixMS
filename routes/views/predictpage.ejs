<!DOCTYPE html>
<html>
<head>
  <title>Hasil Prediksi</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    table { border-collapse: collapse; width: 100%; margin-top: 20px; }
    th, td { border: 1px solid #999; text-align: center; padding: 6px 10px; }
    th { background-color: #f0f0f0; }
    .back-link {
      margin-top: 20px; display: inline-block; text-decoration: none;
      background: #007bff; color: white; padding: 6px 12px; border-radius: 4px;
    }
    .back-link:hover { background: #0056b3; }
  </style>
</head>
<body>

  <h2>=== Prediksi Severity untuk <%= tanggal %> ===</h2>

  <% const hiddenCols = [
    "Acknowledged By",
    "Acknowledgement Status",
    "Alarm Serial Number",
    "Cleared By",
    "Confidence Score",
    "Last Occurred (ST)",
    "Other Information"
  ]; %>

  <% if (rows.length > 0) { %>
    <table>
      <thead>
        <tr>
          <% for (let key in rows[0]) { %>
            <% if (!hiddenCols.includes(key)) { %>
              <th><%= key %></th>
            <% } %>
          <% } %>
        </tr>
      </thead>
      <tbody>
        <% rows.forEach(row => { %>
          <tr>
            <% for (let key in row) { %>
              <% if (!hiddenCols.includes(key)) { %>
                <td><%= row[key] %></td>
              <% } %>
            <% } %>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } else { %>
    <p>Tidak ada data untuk diprediksi.</p>
  <% } %>

  <a href="/<%= username %>/dashboard" class="back-link">← Kembali ke Dashboard</a>

</body>
</html>